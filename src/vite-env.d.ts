/// <reference types="vite/client" />

// Les types Vite/client déclarent les imports `?raw` (ex:
// `import v from '../../../VERSION?raw'`) — audit 2026-08-07 : ce fichier
// manquait, donc `tsc --noEmit` échouait sur TopNav.tsx.
