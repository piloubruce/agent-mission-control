node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Runtime databases
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Python cache
__pycache__/
*.pyc

# Backups / runtime data dirs
backups/
uploads/
data/
sessions/
.tmp_rech/
.tmp/

# Generated artifacts
*.pdf
*.tar.gz

# Editor / temp backups
*.bak-*
*.bak
*~

# --- Runtime / live data (NOT part of a clean install) ---
# Filled blacklist + MC config are regenerated at runtime (server returns {} if missing,
# frontend uses localStorage defaults). Keep them OFF the repo for a clean-install snapshot.
blacklist.json
mc_config.json
mc_session_map.json
mc_favs.json
_models_*.json

# Dead / dev-only files at repo root
apiServer.ts
auto-github-sync.sh
translate.mjs

# Dev-only debug scripts (underscore prefix)
backend/_test_hermes.py
scripts/_*.py
